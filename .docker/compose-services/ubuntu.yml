services:

    ubuntu:
        image: ubuntu
        ports: 
            - ${UBUNTU_PORTS:-80:80}
        volumes: 
            - ../../app:/var/www
        working_dir: /var/www
        command: sleep infinity